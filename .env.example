# Variables d'environnement pour SIF-HPMV

# Port pour le serveur (Railway configure automatiquement)
PORT=5000

# Base de données MongoDB
MONGODB_URI=mongodb://localhost:27017/sif-hpmv

# JWT Secret pour l'authentification
JWT_SECRET=your-super-secret-jwt-key-here

# Environment
NODE_ENV=production

# Frontend build directory (relatif au backend)
FRONTEND_BUILD_PATH=../build

# Upload directory
UPLOAD_DIR=uploads